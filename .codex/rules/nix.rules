# Allow routine Nix checks and builds while working in this trusted flake.
prefix_rule(
    pattern = ["nix", ["eval", "build"]],
    decision = "allow",
    justification = "This repository's Nix evaluations and builds are trusted.",
    match = [
        "nix eval",
        "nix build",
    ],
    not_match = [
        "nix flake update",
        "nix store gc",
    ],
)
